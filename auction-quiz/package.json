{
  "name": "auction-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "Realtime LAN-based auction quiz system for college events",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"green,cyan\" \"npm run dev --prefix server\" \"npm run dev --prefix client -- --host\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
